---
task_id: phase14.5-03
phase: phase14.5-external-runtime-launcher
status: DONE
owner: codex
reviewer: ORCHESTRATOR
priority: high
dependencies:
  - phase14.5-02
execution_mode: REPO_FIRST
allowed_scope:
  - scripts/**
  - tests/scripts/**
  - docs/operations/**
  - coordination/task-board/**
  - coordination/progress/**
  - coordination/delivery/**
forbidden_scope:
  - services/**
  - src/**
  - database/**
  - cloud/**
  - profiles/**
acceptance:
  - Add a deterministic JSON dry-run launcher preflight that never starts a process or changes task lifecycle state.
  - Fail closed for every reject-path category frozen in the Phase 14.5 contract.
  - Add focused tests proving no process launch and deterministic safe output.
  - Document dry-run-only operator usage and the explicit boundary before supervised mode.
expected_artifacts:
  - dry_run_preflight_command
  - reject_path_tests
  - operator_documentation
  - delivery_report
---
# Task Packet

## Objective

Implement the contract-defined dry-run-only launcher preflight.

## Context

Use `docs/operations/phase14.5-launcher-safety-contract.md` as the frozen
contract. This task must not launch OpenCode or any subprocess.

## Constraints

- Never import or call `subprocess`.
- Emit only safe IDs, decision categories, and booleans; do not print paths,
  argv, task content, prompts, credentials, or runtime output.
- Do not create manifests, inboxes, worktrees, scheduler state, or lifecycle changes.

## Implementation Notes

Use stdlib JSON and pure validation functions. Every invalid condition returns
a deterministic `deny_*` decision; success returns `dry_run_ready` only.

## Token And Resource Impact (If Applicable)

No model call, polling loop, process launch, or external network action.

## Validation Steps

Run focused tests, coordination validation, and `git diff --check`.

## Escalation Rules

Stop if implementation needs a runtime launch, shell command, mutable manifest,
or task lifecycle mutation.
